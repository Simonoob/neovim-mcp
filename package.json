{
  "name": "neovim-mcp",
  "version": "0.1.0",
  "description": "MCP server for structured code navigation via Neovim's treesitter and LSP",
  "type": "module",
  "bin": {
    "neovim-mcp": "build/index.js"
  },
  "files": [
    "build"
  ],
  "scripts": {
    "build": "rm -rf build && tsc && cp -r src/lua build/lua && node -e \"require('fs').chmodSync('build/index.js', '755')\"",
    "watch": "tsc --watch",
    "inspector": "npx @modelcontextprotocol/inspector build/index.js"
  },
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.4",
    "neovim": "^5.3.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2"
  }
}
